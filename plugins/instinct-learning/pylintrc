[MASTER]
# Pylint configuration for Instinct-Learning Plugin
# These limits are set based on reasonable complexity for CLI applications

max-locals=20
max-returns=10
max-branches=25
max-statements=80
max-line-length=100

# Disable certain checks that are not relevant for this project
disable=[
    'C0114',  # Missing module docstring (not needed for these modules)
    'R0903',  # Too few public methods (not relevant for scripts)
    'R0904',  # Too few public methods (not relevant for scripts)
]

[DESIGN]
max-args=10
